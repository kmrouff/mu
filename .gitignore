# macOS
.DS_Store

# Raw capture material (phone screenshots / screen recordings) used to produce the assets in
# docs/. Large binaries with no reason to live in the repo — keep the exported result, not the
# source footage.
raw/

# Claude Code: personal, machine-local settings — not meant to be shared
.claude/settings.local.json
